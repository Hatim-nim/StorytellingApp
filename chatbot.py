import customtkinter as ctk

class ChatbotManager:
    def __init__(self, master):
        self.master = master
        self.window = ctk.CTkFrame(master)  # Use a frame instead of Toplevel

    def open_chatbot_window(self):
        # Assuming you handle chatbot interaction here
        pass
